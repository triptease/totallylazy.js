version: 2

jobs:
  ci:
    docker:
      - image: cimg/node:22.0
    steps:
      - checkout
      - run: ./run ci

  test:
    docker:
      - image: cimg/node:22.0
    steps:
      - checkout
      - run: ./run build

workflows:
  version: 2

  master_build:
    jobs:
      - ci:
          filters:
            branches:
              only: master

  non_master_build:
    jobs:
      - test:
          filters:
            branches:
              ignore: master